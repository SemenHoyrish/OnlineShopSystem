<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] @ShopSettings.Name</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container">
                <img src="/Data/site_ico.png" alt="icon" />
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">OnlineShopSystem</a>
                @*@if (OnlineShopSystem.Controllers.AuthController.GetCurrentUserRole((ControllerBase)ViewContext.RouteData.Values["controller"]) == "admin")
                {
                    <a class="navbar-brand" asp-area="" asp-controller="Administration" asp-action="Index">Administration panel</a>
                }*@
                @*<button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>*@
                @*<div class="navbar-collapse collapse d-sm-inline-flex flex-sm-row-reverse">
            <ul class="navbar-nav flex-grow-1">
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
                </li>
            </ul>
        </div>*@
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2021 - @ShopSettings.Name
            @if (ShopSettings.PhoneNumber != "")
            {
                <a>@ShopSettings.PhoneNumber</a>
            }
            @if (ShopSettings.Address != "")
            {
                <a>@ShopSettings.Address</a>
            }
            @if (ShopSettings.Vk != "")
            {
                <a href="https://@ShopSettings.Vk">VK</a>
            }
            @if (ShopSettings.Instagram != "")
            {
                <a href="https://@ShopSettings.Instagram">Instagram</a>
            }
        </div>
        <style>
            footer .container a {
                border-left: 1px solid gray;
                border-left: 1px solid gray;
                padding: 5px;
            }
        </style>
    </footer>
    <script>
        const cont = document.querySelector("header nav .container");

        if (document.cookie.includes("IsAdmin")) {
            cont.innerHTML += `<a class="navbar-brand" href="/Administration/Index">Administration panel</a>`
        }
        if (document.cookie.includes("Session")) {
            cont.innerHTML += `<a class="navbar-brand" href="/Auth/Logout">Logout</a>`
        }
        if (document.cookie.includes("Session")) {
            cont.innerHTML += `<a class="navbar-brand" href="/Basket/Index">Basket</a>`
        }
        if (!document.cookie.includes("Session")) {
            cont.innerHTML += `<a class="navbar-brand" href="/Auth/Login">Login</a>`
            cont.innerHTML += `<a class="navbar-brand" href="/Auth/Register">Register</a>`
        }
    </script>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
